let turn: number, winner: boolean, tie: boolean, board: number[];

function init() {
  board = [0, 0, 0, 0, 0, 0, 0, 0, 0];
  turn = 1;
  tie = false;
  winner = false;
  console.log(turn);
}

init();
